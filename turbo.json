{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", ".output/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "lint:fix": {},
    "format": {},
    "format:check": {},
    "type-check": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "perf:check": {
      "dependsOn": ["build"],
      "cache": false
    },
    "dupes": {
      "inputs": ["src/**", "app/**", "*.jscpd.json", ".jscpd.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
