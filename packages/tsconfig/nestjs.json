{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "NestJS service — Node runtime + decorator metadata (experimentalDecorators, emitDecoratorMetadata) required by @nestjs/* decorators and class-validator.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2024"],
    "types": ["node"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  },
  "exclude": ["node_modules", "dist"]
}
