{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Next.js app — extends app, adds Next plugin and JSX runtime settings. Consumer owns include/exclude.",
  "extends": "./app.json",
  "compilerOptions": {
    "jsx": "preserve",
    "plugins": [
      {
        "name": "next"
      }
    ]
  }
}
