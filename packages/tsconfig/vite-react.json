{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Vite + React app — extends app, adds JSX runtime.",
  "extends": "./app.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
