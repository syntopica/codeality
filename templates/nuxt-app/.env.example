# Copy to .env and fill in. Validated by app/plugins/validateEnv.ts (Zod) at
# startup; the app fails fast if a required variable is missing or malformed.
# Maps to runtimeConfig.public.apiBaseUrl via the NUXT_PUBLIC_ prefix.
NUXT_PUBLIC_API_BASE_URL=https://api.example.com
